{
  "name": "kaleidoscope",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "workspaces": [
    "functions"
  ],
  "devDependencies": {
    "firebase-bolt": "^0.8.4"
  },
  "scripts": {
    "build:database": "firebase-bolt database.rules.bolt -o .database.rules.json",
    "build": "yarn run build:database && yarn workspace functions run build",
    "deploy": "firebase deploy --force",
    "start": "firebase serve -p 3000"
  },
  "author": "",
  "license": "MIT",
  "packageManager": "yarn@4.13.0"
}
